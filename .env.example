# This is the actual URL mirage.js runs on. Local in-browser server so connection string is real, nothing leaking really.
# Needs an actual .env with this env variable to run though.
API_BASE_URL=http://localhost:3000/api
